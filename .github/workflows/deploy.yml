name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Build
        env:
          # Settings > Secrets and variables > Actions 에 등록한 값이 주입됩니다.
          VITE_TOUR_API_KEY:     ${{ secrets.VITE_TOUR_API_KEY }}
          VITE_TOUR_PROXY:       ${{ secrets.VITE_TOUR_PROXY }}
          VITE_KAKAO_JS_KEY:     ${{ secrets.VITE_KAKAO_JS_KEY }}
          VITE_KAKAO_REST_KEY:   ${{ secrets.VITE_KAKAO_REST_KEY }}
          VITE_KAKAO_NATIVE_KEY: ${{ secrets.VITE_KAKAO_NATIVE_KEY }}
          VITE_KAKAO_PROXY:      ${{ secrets.VITE_KAKAO_PROXY }}
          VITE_BASE: /${{ github.event.repository.name }}/
        run: npm run build
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
